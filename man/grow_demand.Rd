% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale_demand.R
\name{grow_demand}
\alias{grow_demand}
\alias{build_demand}
\title{Build multi-year demand from a base-year profile and growth factors}
\usage{
grow_demand(dem, factors, base_year = 2025)

build_demand(
  name = NULL,
  factors,
  base_year = 2025,
  years = NULL,
  regions = NULL,
  from = reneuro::pypsa_eur_41
)
}
\arguments{
\item{dem}{A \code{demand} object.}

\item{factors}{Data frame with columns \code{year}, \code{factor}, and either
\code{region} (matched exactly) or \code{country} (matched to the first two
letters of the region code).}

\item{base_year}{Year of the base profile in \code{dem@demand}. Rows with
that year (or with \code{year} \code{NA}, the year-agnostic form) are the
shape that grows; base-year demand is reproduced exactly where
\code{factor == 1}.}

\item{name}{Name for the built demand object (default: keep the
source object's name).}

\item{years}{Optional years to keep (subset of \code{factors$year});
\code{NULL} uses every year in \code{factors}.}

\item{regions}{Optional region codes to keep.}

\item{from}{Source model holding the base-year demand (default
\link{pypsa_eur_41}).}
}
\value{
The \code{demand} object with a year-keyed \verb{@demand} table.
}
\description{
\code{grow_demand()} is the transparent core: it takes a \code{demand} object
whose \verb{@demand} table holds a base-year profile (region x timeslice,
optionally year-keyed) and a plain growth-factor table, and writes one
profile per factor year — \code{demand * factor}, keyed by \code{year}. Nothing
else changes: the shape is the base year's, the factors are whatever
table you pass (\link{tyndp_demand} filtered to one scenario is one such
table, but any \verb{(country|region, year, factor)} data frame works).
}
\details{
\code{build_demand()} is the convenience wrapper: it pulls a demand object
out of a source model, optionally renames it and restricts it to
\code{regions}, and applies \code{grow_demand()} for the requested \code{years}.
}
\examples{
\dontrun{
ga <- subset(tyndp_demand, scenario == "GA")
d  <- grow_demand(getObjects(pypsa_eur_41)$DEM_ELC, ga, base_year = 2025)
}
}
