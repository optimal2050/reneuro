% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark.R
\name{open_solvers}
\alias{open_solvers}
\title{Open-solver option sets available on this system}
\usage{
open_solvers(check = TRUE)
}
\arguments{
\item{check}{logical. Test each back end, rather than only listing the
option sets.}
}
\value{
A data frame with one row per option set: \code{option}, \code{lang},
\code{solver}, and \code{available} when \code{check = TRUE}.
}
\description{
Lists the \code{energyRt} solver option sets that use an open solver, and reports
which of them this system can run.
}
\details{
Availability is determined per back end: GLPK requires the \code{glpsol}
executable, JuMP requires Julia with the corresponding package, and Pyomo
requires Python with the solver interface. A back end that cannot be reached
is reported rather than silently skipped.
}
\examples{
open_solvers(check = FALSE)
}
\seealso{
\code{\link[=benchmark_solvers]{benchmark_solvers()}}
}
